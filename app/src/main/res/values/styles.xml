<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        windowBackground is what fills the window between it being shown and the
        first onDraw. It must match Y2Palette.DARK.background, or a cold start
        flashes the wrong colour for a frame.
    -->
    <style name="AppTheme" parent="android:style/Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">#0A0D12</item>
    </style>

    <!--
        Selected in MainActivity.onCreate before super, because a theme cannot be
        chosen from a preference in the manifest. Must match Y2Palette.LIGHT.background.
    -->
    <style name="AppTheme.Light" parent="AppTheme">
        <item name="android:windowBackground">#F4F1EA</item>
    </style>
</resources>
